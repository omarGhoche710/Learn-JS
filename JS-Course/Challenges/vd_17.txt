let title_card = "Elzero",
    description_card = "Elzero Web School",
    date_card = "25/10" ;

let markUp = `
  <div>
    <h3>Hello ${title_card}</h3>
    <p>${description_card}</p>
    <span>${date_card}</span>
  </div>
`;

// for(i = 0; i < 4; i++) {
//   document.write(markUp);
// }

document.write(markUp.repeat(4));